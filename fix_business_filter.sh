sed -i 's/const \[selectedBizId, setSelectedBizId\] = useState(businesses\[0\]?.id || '"'"'b1'"'"');/const \[selectedBizId, setSelectedBizId\] = useState<string | null>(null);/g' src/components/BusinessPortalView.tsx
sed -i 's/const currentBiz = businesses.find((b) => b.id === selectedBizId) || businesses\[0\];/const myBusinesses = businesses.filter((b) => b.ownerId === currentUser?.id);\n  const currentBiz = selectedBizId ? myBusinesses.find((b) => b.id === selectedBizId) : myBusinesses[0];\n\n  \/\/ Se o dropdown não tiver selecionado ninguem ainda mas tiver empresa, seleciona a primeira\n  React.useEffect(() => {\n    if (!selectedBizId \&\& myBusinesses.length > 0) {\n      setSelectedBizId(myBusinesses[0].id);\n    }\n  }, [myBusinesses, selectedBizId]);/g' src/components/BusinessPortalView.tsx
sed -i 's/{businesses.map((b) => (/{myBusinesses.map((b) => (/g' src/components/BusinessPortalView.tsx
